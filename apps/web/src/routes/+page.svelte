<!--
  Placeholder landing. The real v1 design ports land in C6+ once
  C2 (tokens + system.css), C3 (bits-ui primitives), and C4 (generated
  TS client wrap) are in place. This page exists to prove the SvelteKit
  + Tailwind v4 + adapter-static pipeline builds; the styling is the
  default Tailwind palette deliberately, not white-label tokens.
-->

<div class="min-h-screen flex items-center justify-center bg-slate-50">
  <main class="text-center">
    <h1 class="text-4xl font-semibold text-slate-900">Flight Academy</h1>
    <p class="mt-2 text-slate-600">Web skeleton — first build.</p>
  </main>
</div>
