{
  "name": "@flight-academy/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tenant-facing SvelteKit app (ADR-014 §A). adapter-static so the bundle embeds into the Rust API binary via the embedded-static feature (ADR-002 §H / ADR-014 §D).",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@flight-academy/web-ui": "workspace:*",
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.63.0",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@tailwindcss/vite": "^4.3.0",
    "@types/node": "^25.9.2",
    "svelte": "^5.56.2",
    "svelte-check": "^4.6.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
