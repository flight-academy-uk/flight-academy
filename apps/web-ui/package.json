{
  "name": "@flight-academy/web-ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared SvelteKit primitives + design tokens (ADR-014 §B). Consumed by apps/web and (later) apps/admin-web. Tokens are the web↔mobile bridge per ADR-014 §C — apps/mobile reads the emitted tokens.dart but not the Svelte primitives.",
  "scripts": {
    "emit-tokens": "bun scripts/emit-tokens-css.ts"
  },
  "exports": {
    "./tokens/tokens.css": "./tokens/tokens.css",
    "./styles/base.css": "./styles/base.css"
  },
  "devDependencies": {
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@fontsource/ibm-plex-sans": "^5.2.8",
    "@types/node": "^25.9.2"
  }
}
